#include<stdio.h>
int main()
{
    //The car y travels 1km in 2min.So, the problem is to solve how min it will take to reach Y the given point.
    int y;
    scanf("%d", &y);
    printf("%d minutos\n", y*2);

    return 0;
}
